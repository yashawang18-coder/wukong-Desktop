<Window x:Class="Wukong.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wukong"
        Width="180"
        Height="180"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove">
    <Grid x:Name="Root">
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="请求摸摸" Click="TouchMenuItem_Click" />
                <MenuItem Header="请求安静" Click="QuietMenuItem_Click" />
                <Separator />
                <MenuItem Header="打开控制面板" Click="OpenPanelMenuItem_Click" />
            </ContextMenu>
        </Grid.ContextMenu>
        <Border Width="128"
                Height="128"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                CornerRadius="64"
                Background="#F7F5F0"
                BorderBrush="#49664E"
                BorderThickness="2">
            <TextBlock Text="悟"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="48"
                       Foreground="#242622" />
        </Border>
    </Grid>
</Window>
