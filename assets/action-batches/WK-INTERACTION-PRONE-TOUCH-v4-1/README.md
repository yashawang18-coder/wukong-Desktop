# Wukong prone-touch v4.1 formal runtime candidate

This package contains the owner-approved continuous PNG sequences for
`wk.interaction.prone_touch`. Playback review is approved, but runtime approval is not.

- `runtime_validation: pending`
- `runtime_approved: false`
- `runtime_use: false`

The package is additive. It does not contain or overwrite the 17 previously approved
keyframes. V3 through V3.4 are rejected and must not be used. Do not register this asset
in runtime until Windows transparent-renderer and state-transition validation pass.
