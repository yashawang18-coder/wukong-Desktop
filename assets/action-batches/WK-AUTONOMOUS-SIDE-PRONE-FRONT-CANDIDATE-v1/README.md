# WK-AUTONOMOUS-SIDE-PRONE-FRONT-CANDIDATE-v1

Candidate side-body prone loop with the head looking toward the owner/screen. It combines approved V3R1 side-prone body phases with approved V4 forward-prone head pixels. F01 and F12 are byte-identical.

Review-only: rear-look-to-front entry and matching exit bridges are still required. It must not hard-cut into V3R1 or replace an approved runtime binding.
